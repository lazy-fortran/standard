INTEGER COUNT
REAL AVERAGE, SUM
LOGICAL DONE

COUNT = 0
SUM = 0.0
DONE = .FALSE.

DO 10 I = 1, 100
    COUNT = COUNT + 1
    SUM = SUM + I
10 CONTINUE

AVERAGE = SUM / COUNT
PRINT 100, COUNT, AVERAGE
100 FORMAT ('COUNT=', I3, ' AVERAGE=', F8.2)
END

